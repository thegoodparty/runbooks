[project]
name = "runbook-scripts"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "anthropic",
    "databricks-sql-connector",
    "pandas",
    "python-dotenv",
    "requests",
    "reportlab",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]
